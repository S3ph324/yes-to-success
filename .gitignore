node_modules
dist
.DS_Store
.env
.env.local
.env.*.local

# Ignore the output video from Git but not videos you import into src/.
out

# AI-generated backgrounds / b-roll frames (regenerated per batch — don't commit)
public/generated-bg
public/broll-frames
public/broll-clips
out/broll-uploads

# Uploaded brand assets and character photos — generated at runtime
public/characters
public/logo-*.png
public/logo-*.jpg
public/logo-*.webp

# Local config that contains secrets (FB tokens, etc) — never commit
config/posting.json

# Vertex AI service account key — NEVER commit
service-account*.json
gcp-credentials*.json
gcp-sa.json
.gcloud/

# Dashboard build artifacts (regenerated)
dashboard/dist
dashboard/node_modules

# Editor / OS
.idea
.vscode
*.swp
*.swo
Thumbs.db

# Logs
*.log
npm-debug.log*

# Shop-listing temp dirs
public/_shoptest
public/_shop-input
public/_advice-avatar
